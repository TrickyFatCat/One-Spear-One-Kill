/// @param is_active
/// @param delta
/// @param scale

var is_active = argument0;
var delta = argument1;
var scale = argument2;

shadow_is_active = is_active;
shadow_delta = delta;
shadow_scale_x = scale;
shadow_scale_y = scale / 2;